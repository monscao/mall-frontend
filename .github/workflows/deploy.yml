name: Frontend Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment mode
        run: |
          echo "Frontend deploy is handled automatically by the EC2 poller."
          echo "Push to main and wait about one minute for the server to sync and redeploy."
