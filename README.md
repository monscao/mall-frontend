# mall-frontend

Webpack + React storefront starter for the Apple Lite mall experience.

## Run

```bash
npm start
```

## Build

```bash
npm run build
```

## Notes

- Dev server runs on `http://localhost:3000`
- Requests to `/api/*` are proxied to `http://localhost:8080`
- Homepage is designed around the `GET /api/home` aggregate endpoint
